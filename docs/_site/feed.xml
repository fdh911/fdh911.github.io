<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-04-10T10:10:55+03:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">fdh911 devlogs</title><subtitle>Windows API, Reverse Engineering, Programming,  Debugging, Cheats &amp; Anti-Cheats, etc.</subtitle><entry><title type="html">Simple DLL Injection via the Windows API</title><link href="http://localhost:4000/2026/04/09/dll-injection-winapi.html" rel="alternate" type="text/html" title="Simple DLL Injection via the Windows API" /><published>2026-04-09T00:00:00+03:00</published><updated>2026-04-09T00:00:00+03:00</updated><id>http://localhost:4000/2026/04/09/dll-injection-winapi</id><content type="html" xml:base="http://localhost:4000/2026/04/09/dll-injection-winapi.html"><![CDATA[<p>All of the code in this article can be found <a href="https://github.com/fdh911/ez_dll_injector">here</a>.</p>

<p>I’ve recently started learning more about the Windows API and gained a surface level understanding of virtual memory, threads, modules, etc. As such, I wanted to apply this newfound knowledge by building a basic DLL injector, which makes use of some simple WinAPI functionality.</p>

<p>Prerequisites: You’re expected to be familiar with the basics of programming in C++ and have a vague idea of how the WinAPI works.</p>

<h1 id="the-final-goal">The final goal</h1>

<p>We want to be able to load a DLL of our choice into any process (Not accounting for security measures right now); a reason why we might want to do this is to read and modify a game’s memory at runtime.</p>

<h1 id="brainstorming">Brainstorming</h1>

<p>Since we have access to the Windows API, the idea is as follows: We’re going to make the target process call the LoadLibrary function on our behalf.</p>

<p>To be more specific: we will be creating a remote thread that runs inside of the process’ address space. The thread will have to call the following function on startup:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">LoadLibraryA</span><span class="p">(</span><span class="s">"c:/path/to/dll"</span><span class="p">);</span>
</code></pre></div></div>

<p>Notice the string literal in the parameters. Since that’s a <code class="language-plaintext highlighter-rouge">const char*</code>,this hints to us that we’ll have to point to an array of bytes that’s inside of the target’s address space.</p>

<p>Which is to say, we will be allocating some memory within that process, copying our DLL’s path into that space, and then freeing it once we’re done with the injection.</p>

<p>We’ll also need to find the address where LoadLibraryA resides within the target process; therefore, we will need to find kernel32.dll’s base address as well.. and that’s about it! Now we can go to the next section:</p>

<h1 id="the-plan">The plan</h1>

<ul>
  <li>Get a handle to the process;</li>
  <li>Allocate some memory in the target;</li>
  <li>Write our DLL path in that memory;</li>
  <li>Find kernel32.dll and the LoadLibraryA address;</li>
  <li>Create a remote thread that then calls LoadLibraryA;</li>
  <li>Free up the memory, close the handles etc.</li>
</ul>

<h1 id="writing-the-code">Writing the code</h1>

<p>To avoid adding unnecessary complexity to this simple example, we’ll directly request the process ID from the user:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">DWORD</span> <span class="n">pid</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Input process ID:</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">pid</span><span class="p">;</span>
</code></pre></div></div>

<p>If you want to automatically find the process by name, you might use an API such as Toolhelp32, in order to go through the list of processes and filter them by exeName. I might make an article on this in the future.</p>

<p>Using this information, we can now request a handle to the target process:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">HANDLE</span> <span class="n">proc_handle</span> <span class="o">=</span> <span class="n">OpenProcess</span><span class="p">(</span><span class="n">PROCESS_ALL_ACCESS</span><span class="p">,</span> <span class="n">FALSE</span><span class="p">,</span> <span class="n">pid</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">proc_handle</span> <span class="o">!=</span> <span class="n">INVALID_HANDLE_VALUE</span><span class="p">);</span>
</code></pre></div></div>

<p>The first parameter indicates what level of permissions we require. This tells the OS whether we want to read virtual memory, write to it, duplicate a handle, and so on. For the sake of this example, we’ll be asking for all the permissions.</p>

<p>Notice the assert after the OpenProcess call. It’s easy to write code that ends up failing for whatever reason, so it’s important to know when and where the failure happens, to make our lives easier when it comes to debugging. (Of course, you should be using a better logging &amp; error handling mechanism than this, but this is sufficient for our purposes).</p>

<p>Reading the DLL path is straightforward:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">dll_name</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Input the full DLL path:</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">dll_name</span><span class="p">;</span>
<span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="kt">size_t</span> <span class="n">dll_name_len</span> <span class="o">=</span> <span class="n">dll_name</span><span class="p">.</span><span class="n">length</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<p>The purpose of <code class="language-plaintext highlighter-rouge">dll_name_len</code> is to make sure that we’ll also be copying the null terminator of the string once we place it in our target’s virtual memory.</p>

<p>Let’s allocate some memory:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">LPVOID</span> <span class="n">memory</span> <span class="o">=</span> <span class="n">VirtualAllocEx</span><span class="p">(</span>
    <span class="n">proc_handle</span><span class="p">,</span> 
    <span class="nb">NULL</span><span class="p">,</span> 
    <span class="n">dll_name_len</span><span class="p">,</span>
    <span class="n">MEM_COMMIT</span> <span class="o">|</span> <span class="n">MEM_RESERVE</span><span class="p">,</span>
    <span class="n">PAGE_READWRITE</span>
<span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">memory</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">);</span>
</code></pre></div></div>

<p>This will commit a piece of memory of size <code class="language-plaintext highlighter-rouge">dll_name_len</code> in the target process, that we can now write the DLL path to:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">std</span><span class="o">::</span><span class="kt">size_t</span> <span class="n">bytes_written</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="n">BOOL</span> <span class="n">write_status</span> <span class="o">=</span> <span class="n">WriteProcessMemory</span><span class="p">(</span>
    <span class="n">proc_handle</span><span class="p">,</span>
    <span class="n">memory</span><span class="p">,</span>
    <span class="n">dll_name</span><span class="p">.</span><span class="n">data</span><span class="p">(),</span>
    <span class="n">dll_name_len</span><span class="p">,</span>
    <span class="o">&amp;</span><span class="n">bytes_written</span>
<span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">write_status</span> <span class="o">==</span> <span class="n">TRUE</span> <span class="o">&amp;&amp;</span> <span class="n">bytes_written</span> <span class="o">==</span> <span class="n">dll_name_len</span><span class="p">);</span>
</code></pre></div></div>

<p>The code is relatively self-explanatory, but we’re writing <code class="language-plaintext highlighter-rouge">dll_name</code> in the process, including the null terminator (so that it’s a valid C string), at the address stored in <code class="language-plaintext highlighter-rouge">memory</code>. We’re checking if the function returned successfully, as well as if we actually wrote all the bytes in the process.</p>

<p>Before we can finally create the remote thread, we need to find the address of LoadLibraryA:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">HMODULE</span> <span class="n">kernel32_handle</span> <span class="o">=</span> <span class="n">GetModuleHandleA</span><span class="p">(</span><span class="s">"Kernel32.dll"</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">kernel32_handle</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">);</span>

<span class="n">FARPROC</span> <span class="n">loadlibrary_address</span> <span class="o">=</span> <span class="n">GetProcAddress</span><span class="p">(</span><span class="n">kernel32_handle</span><span class="p">,</span> <span class="s">"LoadLibraryA"</span><span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">loadlibrary_address</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">);</span>
</code></pre></div></div>

<p>This piece of code actually relies on a querk related to how Windows loads known system DLL’s. You might have noticed that we’re actually finding the address of LoadLibrary within our own process, not within the target process. It turns out that this is perfectly okay: since Kernel32.dll is considered a “KnownDLL”, it’s actually only loaded once by the system, and all processes that want to reference it use the same base module address. This is a very cool topic that I might cover in a future article.</p>

<p>Now we have everything that we need in order to perform the injection itself:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">HANDLE</span> <span class="n">thread_handle</span> <span class="o">=</span> <span class="n">CreateRemoteThread</span><span class="p">(</span>
    <span class="n">proc_handle</span><span class="p">,</span>
    <span class="nb">NULL</span><span class="p">,</span>
    <span class="mi">0</span><span class="p">,</span>
    <span class="p">(</span><span class="n">LPTHREAD_START_ROUTINE</span><span class="p">)</span><span class="n">loadlibrary_address</span><span class="p">,</span>
    <span class="n">memory</span><span class="p">,</span>
    <span class="mi">0</span><span class="p">,</span>
    <span class="nb">NULL</span>
<span class="p">);</span>
<span class="n">assert</span><span class="p">(</span><span class="n">thread_handle</span> <span class="o">!=</span> <span class="nb">NULL</span><span class="p">);</span>

<span class="n">WaitForSingleObject</span><span class="p">(</span><span class="n">thread_handle</span><span class="p">,</span> <span class="n">INFINITE</span><span class="p">);</span>

<span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Successfully injected</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">cin</span><span class="p">.</span><span class="n">get</span><span class="p">();</span>
</code></pre></div></div>

<p>If you are curious what all of the function’s parameters do, I recommend you check the Windows API documentation.</p>

<p>After creating the thread, we wait for it to finish its execution before moving on and declaring the DLL injection as successful.</p>

<p>Once the thread concludes its execution, we don’t need the memory anymore, nor the handles that we’ve opened, so let’s clean everything up:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">VirtualFreeEx</span><span class="p">(</span>
    <span class="n">proc_handle</span><span class="p">,</span>
    <span class="n">memory</span><span class="p">,</span>
    <span class="mi">0</span><span class="p">,</span>
    <span class="n">MEM_RELEASE</span>
<span class="p">);</span>
<span class="n">CloseHandle</span><span class="p">(</span><span class="n">kernel32_handle</span><span class="p">);</span>
<span class="n">CloseHandle</span><span class="p">(</span><span class="n">thread_handle</span><span class="p">);</span>
<span class="n">CloseHandle</span><span class="p">(</span><span class="n">proc_handle</span><span class="p">);</span>

<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
</code></pre></div></div>

<h1 id="testing-it-out">Testing it out</h1>

<p>To verify whether this actually works, we’ll write a very simple DLL which creates a console window that will periodically display a message until we close it. You can find the code <a href="https://github.com/fdh911/ez_dll_injector/blob/master/example_dll/src/Main.cc">here</a>.</p>

<p><img src="/assets/dll_injection1.gif" alt="The code in action" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[All of the code in this article can be found here.]]></summary></entry></feed>